/* =============================================================================
 *  book.js — page-flip engine (leaf computation, rigid CSS flip, responsive).
 *  Implemented in the flip-engine commit.
 * ===========================================================================*/
export function initBook(/* CONFIG */) {
  // placeholder
}
