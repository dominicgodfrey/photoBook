/* =============================================================================
 *  gallery.js — the "See All" thumbnail grid.
 *  Implemented in the gallery commit.
 * ===========================================================================*/
export function initGallery(/* CONFIG */) {
  // placeholder
}
