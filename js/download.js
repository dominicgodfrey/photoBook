/* =============================================================================
 *  download.js — single-photo download + "Download All" as a zip (JSZip CDN).
 *  Implemented in the download commit.
 * ===========================================================================*/
export function downloadOne(/* url, name */) {
  // placeholder
}

export function downloadAll(/* CONFIG */) {
  // placeholder
}
